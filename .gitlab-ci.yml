image: ruby:2.3

variables:
  JEKYLL_ENV: production

pages:
  script:
  - gem install jekyll bundle
  - bundle install
  - jekyll build -d public
  artifacts:
    paths:
    - public
  only:
  - master